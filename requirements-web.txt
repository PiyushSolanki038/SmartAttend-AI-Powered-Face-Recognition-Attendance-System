# Webportal-only deps (Dockerfile) — excludes the desktop app's heavy ML/UI stack
# (customtkinter, opencv-python, face-recognition, dlib-bin, plyer), which webportal/main.py's
# import chain never touches (it imports services/dashboard.py and services/pdf_report.py,
# not services/enrollment.py or services/session.py, which are the ones pulling in ml/).
numpy>=1.24.0
openpyxl>=3.1.0
matplotlib>=3.7.0
Pillow>=10.0.0
reportlab>=4.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
itsdangerous>=2.1.0
jinja2>=3.1.0
python-multipart>=0.0.9
qrcode>=7.4.0
pyotp>=2.9.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
